(() => {
  const STORAGE_KEY='stellar_airspace_site_v1';
  const REFRESH_MS=10000;
  const RADII=[10,25,50,100,150];

  // Tear down any older copy if this script is reloaded.
  if (window.__AIRSPACE_V10__?.destroy) window.__AIRSPACE_V10__.destroy();
  const controller=new AbortController();
  const signal=controller.signal;

  const valid=(lat,lon)=>Number.isFinite(lat)&&Number.isFinite(lon)&&lat>=-90&&lat<=90&&lon>=-180&&lon<=180;
  const readStored=()=>{try{const raw=localStorage.getItem(STORAGE_KEY);if(!raw)return null;const j=JSON.parse(raw);const lat=Number(j.latitude),lon=Number(j.longitude);return valid(lat,lon)?{site_name:j.site_name||'Operation Site',latitude:lat,longitude:lon}:null;}catch(_){return null;}};
  const persist=v=>{try{localStorage.setItem(STORAGE_KEY,JSON.stringify(v));}catch(_){}};
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const fmt=(v,d=0,s='')=>v===null||v===undefined||!Number.isFinite(Number(v))?'—':`${Number(v).toFixed(d)}${s}`;
  const rad=v=>Number(v)*Math.PI/180;
  const hav=(a,b,c,d)=>{const R=6371.0088,p1=rad(a),p2=rad(c),dp=rad(c-a),dl=rad(d-b),x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return R*2*Math.atan2(Math.sqrt(x),Math.sqrt(1-x));};
  const bearing=(a,b,c,d)=>{const p1=rad(a),p2=rad(c),dl=rad(d-b),y=Math.sin(dl)*Math.cos(p2),x=Math.cos(p1)*Math.sin(p2)-Math.sin(p1)*Math.cos(p2)*Math.cos(dl);return(Math.atan2(y,x)*180/Math.PI+360)%360;};
  const diff=(a,b)=>Math.abs(((Number(a)-Number(b)+540)%360)-180);

  const state=window.__AIRSPACE_V10__={
    cfg:readStored(),radius:50,traffic:[],provider:'SERVER ADS-B',status:'IDLE',message:'Waiting for traffic data…',loading:false,lastUpdated:null,selected:null,map:null,raf:0,timer:null,
    destroy(){controller.abort();if(this.timer)clearInterval(this.timer);if(this.map?.isConnected)this.map.remove();delete window.__AIRSPACE_V10__;}
  };

  function trend(a){if(a.track_deg==null||!state.cfg)return'UNKNOWN';const toSite=bearing(a.lat,a.lon,state.cfg.latitude,state.cfg.longitude),d=diff(a.track_deg,toSite);return d<=45?'APPROACHING':d>=135?'DEPARTING':'CROSSING';}
  const severity=d=>d<=10?'CRITICAL':d<=25?'CAUTION':'AWARENESS';

  function styles(){if(document.getElementById('airspace-v10-style'))return;const s=document.createElement('style');s.id='airspace-v10-style';s.textContent=`
.air10-shell{min-height:430px;background:#061117;color:#d9eef7}.air10-setup{min-height:390px;display:flex;align-items:center;justify-content:center;padding:18px}.air10-card{width:min(560px,92%);border:1px solid #143445;background:#07141b;padding:14px}.air10-card h3{margin:0 0 6px;font-size:12px}.air10-card p{margin:0 0 12px;font-size:8px;color:#6c8794}.air10-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px}.air10-grid label:first-child{grid-column:1/-1}.air10-grid label{display:flex;flex-direction:column;gap:4px;font-size:7px;letter-spacing:.08em;color:#4d7d93}.air10-grid input{height:29px;padding:0 8px;border:1px solid #245063;background:#041016;color:#d9eef7}.air10-actions{display:flex;align-items:center;gap:8px;margin-top:10px}.air10-actions button,.air10-controls button,.air10-controls select{height:27px;border:1px solid #245063;background:#07141b;color:#cdeaf6;font-size:8px;padding:0 9px}.air10-error{font-size:8px;color:#ff7777}
.air10-top{display:grid;grid-template-columns:minmax(150px,1.2fr) minmax(120px,.8fr) 130px auto;border-bottom:1px solid #143445;background:#07141b}.air10-cell{padding:7px 9px;border-right:1px solid #143445;min-width:0}.air10-cell small,.air10-summary small{display:block;font-size:7px;letter-spacing:.08em;color:#4d7d93}.air10-cell b{display:block;margin-top:2px;font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.air10-controls{display:flex;align-items:center;gap:6px;padding:6px 8px}.air10-controls .primary{border-color:#2f7894;color:#29c8f0}
.air10-layout{display:grid;grid-template-columns:minmax(0,1.7fr) minmax(330px,.8fr);min-height:370px}.air10-map-host{position:relative;min-height:370px;background:#031017;border-right:1px solid #143445;overflow:hidden}.air10-placeholder{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;color:#315b6d;font-size:8px}.air10-side{min-width:0;display:flex;flex-direction:column}.air10-summary{display:grid;grid-template-columns:repeat(4,1fr);border-bottom:1px solid #143445}.air10-summary>div{padding:8px;border-right:1px solid #143445}.air10-summary strong{display:block;margin-top:3px;font-size:13px}.air10-summary em{display:block;margin-top:2px;font-size:7px;color:#6c8794;font-style:normal}.air10-list{overflow:auto;max-height:270px}.air10-table{width:100%;border-collapse:collapse;font-size:8px}.air10-table th{position:sticky;top:0;background:#07141b;color:#4d7d93;text-align:left;padding:6px;border-bottom:1px solid #143445}.air10-table td{padding:6px;border-bottom:1px solid #102b38}.air10-table tbody tr{cursor:pointer}.air10-table tbody tr:hover,.air10-table tr.selected{background:#0a1b23}.air10-table code{font-size:9px;color:#d9eef7}.air10-table small{display:block;margin-top:2px;color:#6c8794}.air10-critical{color:#ff6767}.air10-caution{color:#e9ae42}.air10-awareness{color:#64cceb}.air10-empty{padding:16px;color:#6c8794;font-size:9px}.air10-foot{margin-top:auto;padding:7px 9px;border-top:1px solid #143445;color:#6c8794;font-size:7px}.air10-foot b{color:#e9ae42}
#airspace-v10-map{position:fixed;display:none;z-index:20;overflow:hidden;background:#07141b}#airspace-v10-map .tile{position:absolute;width:256px;height:256px;user-select:none;pointer-events:none}#airspace-v10-map .ring{position:absolute;border:1px solid rgba(41,200,240,.72);border-radius:50%;transform:translate(-50%,-50%)}#airspace-v10-map .ring.inner{border-color:rgba(233,174,66,.72)}#airspace-v10-map .ring.critical{border-color:rgba(255,103,103,.82)}#airspace-v10-map .site{position:absolute;width:11px;height:11px;border:2px solid #fff;background:#29c8f0;border-radius:50%;transform:translate(-50%,-50%)}#airspace-v10-map .plane{position:absolute;transform:translate(-50%,-50%);border:0;background:transparent;color:#d9eef7;font-size:17px;cursor:pointer;text-shadow:0 0 3px #000}#airspace-v10-map .plane.caution{color:#e9ae42}#airspace-v10-map .plane.critical{color:#ff6767}#airspace-v10-map .maplabel,#airspace-v10-map .updated{position:absolute;left:8px;padding:4px 6px;background:rgba(3,13,18,.88);border:1px solid #143445;color:#7ea6b7;font-size:7px;z-index:3}#airspace-v10-map .maplabel{top:8px}#airspace-v10-map .updated{bottom:6px}#airspace-v10-map .attrib{position:absolute;right:4px;bottom:3px;padding:2px 4px;background:rgba(255,255,255,.78);color:#222;font-size:7px;z-index:3}
@media(max-width:1100px){.air10-layout{grid-template-columns:1fr}.air10-map-host{border-right:0;border-bottom:1px solid #143445}.air10-top{grid-template-columns:1fr 1fr}.air10-controls{grid-column:1/-1}.air10-summary{grid-template-columns:1fr 1fr}}
`;document.head.appendChild(s);}

  function setupBody(){return `<div class="air10-shell"><div class="air10-setup"><div class="air10-card"><h3>SET OPERATION SITE</h3><p>Enter the coordinates used as the centre of the traffic monitor.</p><div class="air10-grid"><label>SITE NAME<input id="air10-name" value="${esc(state.cfg?.site_name||'Operation Site')}"></label><label>LATITUDE<input id="air10-lat" type="number" step="any" value="${state.cfg?.latitude??24.2601511}"></label><label>LONGITUDE<input id="air10-lon" type="number" step="any" value="${state.cfg?.longitude??55.789476}"></label></div><div class="air10-actions"><button id="air10-save" type="button">SAVE LOCATION</button><span id="air10-error" class="air10-error"></span></div></div></div></div>`;}
  function panelBody(){if(!state.cfg)return setupBody();const ac=state.traffic,near=ac[0]||null,crit=ac.filter(a=>a.distance_km<=10).length,caut=ac.filter(a=>a.distance_km>10&&a.distance_km<=25).length,app=ac.filter(a=>trend(a)==='APPROACHING').length;const rows=ac.slice(0,40).map(a=>{const sev=severity(a.distance_km);return `<tr data-air10-hex="${esc(a.hex)}" class="${state.selected===a.hex?'selected':''}"><td><code>${esc(a.callsign)}</code><small>${esc(a.aircraft_type||a.registration||'—')}</small></td><td class="air10-${sev.toLowerCase()}">${fmt(a.distance_km,1,' km')}<small>${fmt(a.bearing_deg,0,'°')}</small></td><td>${fmt(a.altitude_ft,0,' ft')}<small>${fmt(a.ground_speed_kmh,0,' km/h')}</small></td><td>${fmt(a.track_deg,0,'°')}<small>${trend(a)}</small></td></tr>`;}).join('');const updated=state.lastUpdated?new Date(state.lastUpdated).toISOString().slice(11,19)+'Z':'—';return `<div class="air10-shell"><div class="air10-top"><div class="air10-cell"><small>OPERATION SITE</small><b>${esc(state.cfg.site_name)}</b></div><div class="air10-cell"><small>TRAFFIC SOURCE</small><b>${esc(state.provider)}</b></div><div class="air10-cell"><small>LAST UPDATE</small><b>${updated}</b></div><div class="air10-controls"><select id="air10-radius">${RADII.map(v=>`<option value="${v}" ${v===state.radius?'selected':''}>${v} km</option>`).join('')}</select><button id="air10-refresh" class="primary" type="button">${state.loading?'UPDATING…':'REFRESH'}</button><button id="air10-change" type="button">LOCATION</button></div></div><div class="air10-layout"><div class="air10-map-host" id="air10-map-host"><div class="air10-placeholder">${esc(state.message)}</div></div><div class="air10-side"><div class="air10-summary"><div><small>OBSERVED</small><strong>${ac.length}</strong><em>${state.radius} km radius</em></div><div><small>NEAREST</small><strong>${near?fmt(near.distance_km,1,' km'):'—'}</strong><em>${esc(near?.callsign||'no target')}</em></div><div><small>APPROACHING</small><strong>${app}</strong><em>track assessment</em></div><div><small>ALERTS</small><strong class="${crit?'air10-critical':caut?'air10-caution':'air10-awareness'}">${crit?crit+' CRIT':caut?caut+' CAUT':'NONE'}</strong><em>${esc(state.status)}</em></div></div><div class="air10-list">${rows?`<table class="air10-table"><thead><tr><th>CALLSIGN</th><th>DIST / BRG</th><th>ALT / SPEED</th><th>TRACK / TREND</th></tr></thead><tbody>${rows}</tbody></table>`:`<div class="air10-empty">${esc(state.message)}</div>`}</div><div class="air10-foot"><b>OBSERVATIONAL ONLY.</b> Absence of an ADS-B/MLAT target does not mean the airspace is clear. CAA/AIS/ATC and NOTAM verification remain authoritative.</div></div></div></div>`;}
  function airspacePanel(item){return panelShell(item,panelBody(),'LIVE TRAFFIC · 10 S UPDATE · SITUATIONAL AWARENESS');}

  async function loadTraffic(){if(!state.cfg||state.loading)return;state.loading=true;state.status='UPDATING';state.message='Retrieving live ADS-B / MLAT observations…';renderWorkspace();try{const q=new URLSearchParams({radius_km:String(state.radius),lat:String(state.cfg.latitude),lon:String(state.cfg.longitude),site_name:state.cfg.site_name,_:String(Date.now())});const r=await fetch(`/api/airspace/traffic?${q}`,{cache:'no-store',signal});const j=await r.json();if(!r.ok||!j.ok)throw new Error(j.message||`HTTP ${r.status}`);state.traffic=(j.traffic?.aircraft||[]).map(a=>({...a,distance_km:Number(a.distance_km),bearing_deg:Number(a.bearing_deg)})).sort((a,b)=>a.distance_km-b.distance_km);state.provider=j.traffic?.provider||'SERVER ADS-B';state.status=j.status||'OBSERVATIONAL';state.message=state.traffic.length?`${state.traffic.length} targets observed.`:'No ADS-B / MLAT targets observed in this radius.';state.lastUpdated=Date.now();}catch(e){if(e.name!=='AbortError'){state.status='UNAVAILABLE';state.message=`Traffic refresh failed: ${e.message}`;}}finally{state.loading=false;renderWorkspace();queueMap(true);}}

  const zoomFor=r=>r<=12?11:r<=28?10:r<=60?9:r<=120?8:7;
  function worldPixel(lat,lon,z){const scale=256*2**z,x=(lon+180)/360*scale,s=Math.sin(rad(lat)),y=(0.5-Math.log((1+s)/(1-s))/(4*Math.PI))*scale;return{x,y};}
  function ensureMap(){if(!state.map){state.map=document.createElement('div');state.map.id='airspace-v10-map';document.body.appendChild(state.map);}return state.map;}
  function drawMap(){state.raf=0;const host=document.getElementById('air10-map-host');if(!state.cfg||!host){if(state.map)state.map.style.display='none';return;}const map=ensureMap(),r=host.getBoundingClientRect();if(r.width<20||r.height<20||r.bottom<0||r.top>innerHeight){map.style.display='none';return;}map.style.display='block';map.style.left=`${Math.round(r.left)}px`;map.style.top=`${Math.round(r.top)}px`;map.style.width=`${Math.round(r.width)}px`;map.style.height=`${Math.round(r.height)}px`;map.replaceChildren();const w=r.width,h=r.height,z=zoomFor(state.radius),center=worldPixel(state.cfg.latitude,state.cfg.longitude,z),left=center.x-w/2,top=center.y-h/2,n=2**z;for(let tx=Math.floor(left/256)-1;tx<=Math.floor((left+w)/256)+1;tx++)for(let ty=Math.floor(top/256)-1;ty<=Math.floor((top+h)/256)+1;ty++){if(ty<0||ty>=n)continue;const x=((tx%n)+n)%n,img=document.createElement('img');img.className='tile';img.alt='';img.src=`https://tile.openstreetmap.org/${z}/${x}/${ty}.png`;img.style.left=`${tx*256-left}px`;img.style.top=`${ty*256-top}px`;map.appendChild(img);}const mpp=156543.03392*Math.cos(rad(state.cfg.latitude))/2**z;[[state.radius,''],[25,'inner'],[10,'critical']].forEach(([km,klass])=>{if(km>state.radius)return;const ring=document.createElement('div'),d=Number(km)*2000/mpp;ring.className=`ring ${klass}`;ring.style.cssText=`width:${d}px;height:${d}px;left:${w/2}px;top:${h/2}px`;map.appendChild(ring);});const site=document.createElement('div');site.className='site';site.style.left=`${w/2}px`;site.style.top=`${h/2}px`;map.appendChild(site);state.traffic.forEach(a=>{const p=worldPixel(a.lat,a.lon,z),x=p.x-left,y=p.y-top;if(x<-20||x>w+20||y<-20||y>h+20)return;const b=document.createElement('button'),sev=severity(a.distance_km);b.type='button';b.className=`plane ${sev==='CRITICAL'?'critical':sev==='CAUTION'?'caution':''}`;b.dataset.hex=a.hex;b.textContent='✈';b.style.left=`${x}px`;b.style.top=`${y}px`;b.style.rotate=`${Number(a.track_deg||0)}deg`;b.title=`${a.callsign} · ${fmt(a.distance_km,1,' km')}`;map.appendChild(b);});const label=document.createElement('div');label.className='maplabel';label.textContent=`${state.cfg.site_name} · ${state.radius} km monitor`;map.appendChild(label);const upd=document.createElement('div');upd.className='updated';upd.textContent=`LIVE · ${state.lastUpdated?new Date(state.lastUpdated).toISOString().slice(11,19)+'Z':'waiting'}`;map.appendChild(upd);const at=document.createElement('div');at.className='attrib';at.textContent='© OpenStreetMap contributors';map.appendChild(at);}
  function queueMap(force=false){if(force&&state.raf){cancelAnimationFrame(state.raf);state.raf=0;}if(!state.raf)state.raf=requestAnimationFrame(drawMap);}

  document.addEventListener('click',e=>{const t=e.target instanceof Element?e.target.closest('button,tr'):null;if(!t)return;if(t.id==='air10-save'){e.preventDefault();e.stopPropagation();const lat=Number(document.getElementById('air10-lat')?.value),lon=Number(document.getElementById('air10-lon')?.value),name=(document.getElementById('air10-name')?.value||'Operation Site').trim();if(!valid(lat,lon)){const er=document.getElementById('air10-error');if(er)er.textContent='Enter valid coordinates.';return;}state.cfg={site_name:name||'Operation Site',latitude:lat,longitude:lon};persist(state.cfg);state.traffic=[];renderWorkspace();queueMap(true);loadTraffic();return;}if(t.id==='air10-refresh'){loadTraffic();return;}if(t.id==='air10-change'){state.traffic=[];state.selected=null;state.status='IDLE';state.message='Waiting for traffic data…';try{localStorage.removeItem(STORAGE_KEY);}catch(_){}state.cfg=null;if(state.map)state.map.style.display='none';renderWorkspace();return;}if(t.dataset?.air10Hex){state.selected=t.dataset.air10Hex;renderWorkspace();queueMap(true);return;}if(t.classList?.contains('plane')&&t.dataset.hex){state.selected=t.dataset.hex;renderWorkspace();queueMap(true);}}, {capture:true,signal});
  document.addEventListener('change',e=>{if(e.target?.id==='air10-radius'){state.radius=Number(e.target.value)||50;state.traffic=[];renderWorkspace();queueMap(true);loadTraffic();}}, {signal});

  styles();PANEL_NAMES.airspace='AIRSPACE & LIVE TRAFFIC';renderers.airspace=airspacePanel;const baseRender=renderWorkspace;renderWorkspace=function(){baseRender();queueMap();};new MutationObserver(()=>queueMap()).observe(document.getElementById('workspace'),{childList:true,subtree:true});addEventListener('resize',()=>queueMap(),{passive:true,signal});addEventListener('scroll',()=>queueMap(),{passive:true,capture:true,signal});const requested=new URLSearchParams(location.search).get('panel');if(requested==='airspace'){document.body.classList.add('popout');layout=[{panel:'airspace',span:3,order:0}];}else if(!layout.some(x=>x.panel==='airspace')){const i=layout.findIndex(x=>x.panel==='command'),at=i>=0?i+1:layout.length;layout.splice(at,0,{panel:'airspace',span:2,order:at});layout.forEach((x,n)=>x.order=n);}renderWorkspace();queueMap(true);if(state.cfg)loadTraffic();state.timer=setInterval(()=>{if(state.cfg)loadTraffic();},REFRESH_MS);
})();