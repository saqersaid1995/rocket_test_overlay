(()=>{const root=document.querySelector('.planning-shell'),id=root.dataset.operationId,message=document.querySelector('#planning-message');
async function post(url,data){message.textContent='PROCESSING CONTROLLED CHANGE…';message.className='';const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),body=await response.json();if(!response.ok){message.textContent=body.error;message.className='error';return null}message.textContent='PLANNING RECORD UPDATED';message.className='ok';return body}
document.querySelector('#generate-plan').onclick=async()=>{const replace=document.querySelectorAll('.task').length>0;if(replace&&!confirm('Regenerate all planning tasks from T0? Current task status changes will be replaced.'))return;const result=await post(`/api/ops/${id}/planning/generate`,{replace});if(result)location.reload()};
document.querySelectorAll('.department-index button').forEach(button=>button.onclick=()=>{document.querySelectorAll('.department-index button').forEach(x=>x.classList.remove('active'));button.classList.add('active');document.querySelectorAll('.task').forEach(task=>task.hidden=button.dataset.filter!=='ALL'&&task.dataset.department!==button.dataset.filter)});
document.querySelectorAll('.save-task').forEach(button=>button.onclick=async()=>{const task=button.closest('.task'),result=await post(`/api/ops/${id}/planning/tasks/${task.dataset.task}`,{assigned_person:task.querySelector('.assigned').value,status:task.querySelector('.status').value,blocker:task.querySelector('.blocker').value});if(result)location.reload()});
})();
